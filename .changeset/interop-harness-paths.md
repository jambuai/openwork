---
"@jambulab/openwork": minor
---

Discover, load, and watch skills and slash commands from interoperable project harness directories (`.cursor`, `.agents`, `.gemini`) in addition to `.claude`, including user home trees for Cursor/Codex/Gemini layouts, `--add-dir` paths, dynamic skill discovery, filesystem permission scoping, and sandbox write blocks for the same skill roots.
